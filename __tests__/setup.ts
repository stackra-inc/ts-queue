/**
 * Vitest global test setup for @stackra/ts-queue.
 *
 * Runs before every test file. Use for:
 * - Global mocks
 * - Polyfills
 * - Test environment configuration
 * - Metadata cleanup
 *
 * @module @stackra/ts-queue/test-setup
 */

import "reflect-metadata";

/**
 * @fileoverview injectables barrel export.
 *
 * Exported: {@link queue}.
 *
 * injectables are typed lazy proxies — see
 * `.kiro/steering/facade-pattern.md` for the full pattern.
 *
 * @module facades
 * @category injectables
 */

export { queue } from "./queue.facade";
